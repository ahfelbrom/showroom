{% extends "show/base.html.twig" %}

{% block pageHeading %}
    Create a show
{% endblock %}

{% block content %}

    {{ form_start(form, {'attr': {'id': 'form-show', 'novalidate':"novalidate"}}) }} {# novalidate pour empêcher la validation par html5 #}
    {# {{ form_widget(form) }} #}

    {# {{ form_row(form.name) }} #}
    {{ form_label(form.name) }}
    {{ form_widget(form.name) }}
    {{ form_errors(form.name) }}
    {{ form_rest(form) }}
    <input type="submit" name="Créer" value="Créer">
    {{ form_end(form) }}

{% endblock %}