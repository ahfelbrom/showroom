<!-- Search Widget -->
<div class="card my-4">
    <h5 class="card-header">Search</h5>
    <div class="card-body">
        <form action="{{ path('show_search') }}" method="post">
            <div class="input-group">
                <input type="text" id="category_name" name="name" required="required" class="form-control" placeholder="Search for..." />
                <span class="input-group-btn">
                    <button class="btn btn-secondary" type="submit">Go!</button>
                </span>
            </div>
        </form>
    </div>
</div>